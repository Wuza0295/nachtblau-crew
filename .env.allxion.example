# Allxion static build (Webspace + Android WebView). Copy to .env.allxion locally.
# Backend stays on Manus — set to your deployed app URL (OAuth + tRPC).
VITE_API_ORIGIN=https://nachtblau-crew.manus.space
