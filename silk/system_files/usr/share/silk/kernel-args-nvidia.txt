# NVIDIA – empfohlene Kernel-Args (als root, einmalig):
#   rpm-ostree kargs --append=nvidia-drm.modeset=1
#
# Wichtig: Für NVIDIA-GPUs das Silk-Image auf nvidia-open Basis nutzen:
#   sudo bootc switch --enforce-container-sigpolicy ghcr.io/<user>/silk-nvidia-open:latest
#
# Upstream-Basis: ghcr.io/ublue-os/aurora-nvidia-open:stable (Turing/RTX 16xx+).
# Ältere Pascal/Maxwell (GTX 9xx/10xx): ggf. Bazzite-nvidia (siehe README).
