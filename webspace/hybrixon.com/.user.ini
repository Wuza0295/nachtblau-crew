; ALL-INKL / PHP-FPM — large per-file uploads + multi-select (15 images + 15 videos)
; Sequential staging uploads one file at a time; keep headroom for a single 50 MB video.
upload_max_filesize = 64M
post_max_size = 96M
max_file_uploads = 40
max_execution_time = 300
memory_limit = 256M
