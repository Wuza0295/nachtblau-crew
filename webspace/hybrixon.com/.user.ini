; ALL-INKL / PHP-FPM — large per-file uploads + multi-select (15 images + 15 videos)
; Sequential staging: one file per request; headroom for a single ~200 MB video.
upload_max_filesize = 220M
post_max_size = 256M
max_file_uploads = 40
max_execution_time = 600
memory_limit = 512M
