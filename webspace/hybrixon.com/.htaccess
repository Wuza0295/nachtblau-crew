Options -MultiViews
DirectoryIndex spa/index.html index.php
RewriteEngine On

# ALL-INKL: PHP 8.5 (CGI/FPM)
AddHandler php85-cgi .php

# Basic hardening
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
  # Camera stays off globally; age-verify.php / yoti-callback.php open it for facial AVS.
  Header set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# www → apex
RewriteCond %{HTTP_HOST} ^www\.hybrixon\.com$ [NC]
RewriteRule ^(.*)$ https://hybrixon.com/$1 [R=301,L]

# Protect runtime / source
RewriteRule ^data/ - [F,L]
RewriteRule ^includes/ - [F,L]

# JSON API (PHP 8.5 engine)
RewriteRule ^api(?:/(.*))?$ api/index.php [L,QSA]

# Keep classic PHP endpoints reachable (uploads are denied via uploads/.htaccess)
RewriteRule ^(admin|assets/img|assets/css|assets/js)(/|$) - [L]
RewriteRule ^(media|profile-media|messages|message|age-verify|yoti-callback|profile|u|rules|terms|privacy|impressum|login|logout|register|compose)\.php$ - [L]
RewriteRule ^manifest\.json$ - [L]

# SPA build output
RewriteRule ^spa/ - [L]
RewriteRule ^spa-assets/ - [L]

# Client routes → Vite SPA (React 19)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ spa/index.html [L]
